[package]
name = "integration-tests"
version = "0.0.0"
publish = false
edition.workspace = true

[dev-dependencies]
rscni-plugin = { path = "../../plugin" }
serde_json = { workspace = true }
tempfile = { workspace = true }

[lints]
workspace = true
